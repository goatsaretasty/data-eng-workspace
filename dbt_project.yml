name: nyc_taxi_pipeline
version: "1.0.0"
config-version: 2

profile: nyc_taxi_pipeline

model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  nyc_taxi_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
